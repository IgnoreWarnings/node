/* Filewriter
 *
 * Author: Pascal Bauer <pascal.bauer@rwth-aachen.de>
 *
 * SPDX-FileCopyrightText: 2023-2024 Pascal Bauer <pascal.bauer@rwth-aachen.de>
 * SPDX-License-Identifier: Apache-2.0
 */

#include <fstream>
#include <iostream>

void write_to_file(std::string data, const std::filesystem::path file) {

  // std::string stringToWrite = "$device";
  // std::string filePath =
  //     "/some/path/to/file"; // Change this to your desired file path

  // // Create an ofstream object to write to the file
  // std::ofstream outputFile(/ sys / bus / platform / drivers / $DRIVER / unbind);

  // // Check if the file opened successfully
  // if (outputFile.is_open()) {
  //   // Write the string to the file
  //   outputFile << stringToWrite;

  //   // Close the file
  //   outputFile.close();
  //   std::cout << "Successfully wrote to " << filePath << std::endl;
  // } else {
  //   std::cerr << "Error opening file " << filePath << std::endl;
  // }
}